[package]
name = "rabbitmq-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { version = "1", features = ["full"] }
lapin = "2.5.3"  # Updated to match the version your code expects
thiserror = "1.0"
futures-util = "0.3.31"
# Add these dependencies which are needed for the tokio integration
amq-protocol-uri = "7.2.3"  # For AMQPUri